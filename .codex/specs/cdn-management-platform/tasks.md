# Implementation Plan

- [x] 1. 搭建后端基础工程与配置加载骨架
  - 初始化 Go + Gin + GORM + Redis 的目录结构，至少包含 `cmd/`、`internal/handler/`、`internal/middleware/`、`internal/service/`、`internal/repository/`、`internal/provider/`、`internal/model/`、`internal/infra/`
  - 编写配置加载模块，支持数据库、Redis、JWT 或会话密钥、应用级加密密钥、初始化超级管理员参数
  - 实现 Gin 应用启动入口、基础路由注册、健康检查接口与统一错误响应结构
  - _Requirements: 5.1, 8.1, 9.1, 9.3_

- [x] 2. 实现核心数据模型与数据库迁移
  - [x] 2.1 编写用户、项目、用户项目角色、项目存储桶、项目 CDN、审计日志的数据模型与 GORM 迁移代码
    - 创建 `users`、`projects`、`user_project_roles`、`project_buckets`、`project_cdns`、`audit_logs` 模型
    - 添加唯一约束、索引、枚举值约束与创建时间字段
    - _Requirements: 3.1, 3.2, 4.1, 4.2, 6.1, 9.2_
  - [x] 2.2 实现仓储层基础查询接口与事务封装
    - 为用户、项目、项目绑定、审计日志编写 repository 接口与 GORM 实现
    - 实现服务层可复用的事务执行器
    - _Requirements: 3.1, 3.2, 4.2, 6.1, 9.2_
  - [x] 2.3 为核心模型与仓储查询补充最小化自动化测试
    - 覆盖唯一约束、项目角色绑定、项目资源关联查询
    - _Requirements: 3.2, 4.2, 6.1, 9.2_

- [x] 3. 实现初始化、认证与密码安全能力
  - [x] 3.1 编写系统首次启动超级管理员初始化逻辑
    - 检查空白初始化数据状态
    - 创建超级管理员账号并写入初始化审计日志
    - _Requirements: 5.1, 5.2, 5.3, 6.1_
  - [x] 3.2 实现密码摘要、登录认证、当前用户信息查询与修改密码接口
    - 编写密码摘要与校验工具
    - 实现 `login`、`me`、`change-password` 的 handler、service、repository 调用链
    - _Requirements: 4.1, 4.3, 4.4, 8.1, 8.3_
  - [x] 3.3 实现认证中间件与统一身份上下文注入
    - 解析登录态
    - 将用户身份注入请求上下文，供后续 RBAC 与项目作用域中间件复用
    - _Requirements: 4.3, 8.2, 8.3_
  - [x] 3.4 为初始化与认证流程补充最小化自动化测试
    - 覆盖首次初始化、登录成功、密码修改成功
    - _Requirements: 5.1, 5.3, 4.4, 8.3_

- [x] 4. 实现 RBAC 与项目作用域授权链路
  - [x] 4.1 编写平台角色与项目角色枚举、授权判定器与权限检查中间件
    - 区分平台管理员、项目管理员、项目只读用户
    - 为读操作与写操作分别定义权限断言
    - _Requirements: 4.2, 4.3, 4.5, 8.2, 8.3_
  - [x] 4.2 实现项目作用域校验中间件与项目授权查询缓存
    - 校验目标项目是否在当前用户授权范围内
    - 使用 Redis 缓存用户项目角色快照
    - _Requirements: 3.3, 3.4, 4.2, 4.5, 8.2, 9.3_
  - [x] 4.3 将越权拦截与拒绝访问写入审计日志
    - 记录角色不足与项目越权两类拒绝事件
    - _Requirements: 3.4, 4.5, 6.1, 6.5, 8.3, 8.4_
  - [x] 4.4 为权限判定与项目越权拦截补充最小化自动化测试
    - 覆盖平台管理员、项目管理员、项目只读用户三类角色
    - _Requirements: 3.4, 4.5, 6.5, 8.2_

- [x] 5. 实现平台管理员的用户与项目管理接口
  - [x] 5.1 编写用户创建、编辑、禁用、删除与项目角色绑定接口
    - 实现用户状态维护与项目角色批量绑定更新
    - _Requirements: 4.1, 4.2, 4.3, 4.5_
  - [x] 5.2 编写项目创建、查询、编辑、删除接口
    - 保存项目名称、描述、创建时间
    - _Requirements: 3.1, 3.3, 8.4_
  - [x] 5.3 在项目配置接口中实现 1~2 个存储桶与 1~2 个 CDN 地址绑定约束
    - 校验绑定数量、主绑定标记与数据一致性
    - _Requirements: 2.1, 3.2, 3.3_
  - [x] 5.4 为用户管理与项目配置约束补充最小化自动化测试
    - 覆盖禁用用户、重复绑定、绑定数量越界
    - _Requirements: 3.2, 4.3, 4.5_

- [x] 6. 实现云厂商 Provider 抽象与存储桶连接管理
  - [x] 6.1 定义对象存储与 CDN Provider 统一接口、错误模型与厂商类型枚举
    - 为后续四大云厂商适配器建立统一调用边界
    - _Requirements: 1.2, 2.2, 2.3, 8.3, 9.1, 9.4_
  - [x] 6.2 编写存储桶凭证加密、解密与脱敏输出能力
    - 在保存时加密，在读取时按最小必要原则脱敏返回
    - _Requirements: 1.1, 8.1, 8.4_
  - [x] 6.3 实现存储桶连接校验与云厂商自动识别服务
    - 基于 AccessKey 与目标存储桶信息探测受支持云厂商
    - 保存识别后的 `provider_type`
    - _Requirements: 1.1, 1.2, 1.5_
  - [x] 6.4 为凭证加密与厂商识别逻辑补充最小化自动化测试
    - 覆盖凭证脱敏输出与连接失败错误映射
    - _Requirements: 1.2, 1.5, 8.1_

- [x] 7. 实现项目内存储桶文件管理能力
  - [x] 7.1 编写项目存储桶文件列表查询接口
    - 通过统一 Provider 返回对象列表与基础元数据
    - _Requirements: 1.3, 4.5, 7.1_
  - [x] 7.2 编写文件上传、下载、删除、重命名接口
    - 对接统一 Provider 执行对象操作
    - 写入成功、失败与拒绝访问审计日志
    - _Requirements: 1.4, 3.4, 6.1, 8.4_
  - [x] 7.3 在存储桶资源页面所需接口中加入审计查询入口
    - 提供按项目与资源路径筛选的审计查询能力
    - _Requirements: 6.3, 6.4, 6.5_
  - [x] 7.4 为存储桶文件操作服务补充最小化自动化测试
    - 覆盖上传、删除、重命名与越权访问
    - _Requirements: 1.4, 3.4, 6.1, 8.4_

- [x] 8. 实现项目内 CDN 配置、刷新与资源同步能力
  - [x] 8.1 编写项目 CDN 地址配置读写接口
    - 允许每项目配置 1~2 个 CDN 地址并保存厂商类型
    - _Requirements: 2.1, 3.2, 3.3_
  - [x] 8.2 编写 URL 刷新与目录刷新接口
    - 调用统一 CDN Provider
    - 记录刷新结果与错误原因
    - _Requirements: 2.2, 2.3, 2.5, 6.1_
  - [x] 8.3 编写资源同步编排接口与短时任务状态缓存
    - 组合存储桶最新资源发布与 CDN 缓存更新流程
    - 使用 Redis 保存受理状态与短时任务结果
    - _Requirements: 2.4, 2.5, 8.4, 9.3, 9.4_
  - [x] 8.4 为刷新与资源同步逻辑补充最小化自动化测试
    - 覆盖 URL 刷新、目录刷新、同步失败错误映射
    - _Requirements: 2.2, 2.3, 2.4, 2.5_

- [x] 9. 实现审计日志查询接口与统一审计记录器
  - [x] 9.1 编写服务层审计记录器并接入关键业务流程
    - 统一记录用户、项目、目标资源、结果、时间、请求标识与元数据
    - _Requirements: 6.1, 8.4_
  - [x] 9.2 编写平台级审计查询接口与项目级审计查询接口
    - 支持按项目、用户、操作类型、时间范围筛选
    - 按平台管理员与项目管理员角色限制可见范围
    - _Requirements: 6.2, 6.3, 6.5_
  - [x] 9.3 为审计记录与查询权限补充最小化自动化测试
    - 覆盖平台级查询、项目级查询、越权查询拒绝
    - _Requirements: 6.2, 6.3, 6.5_

- [x] 10. 搭建前端基础工程与三主题框架
  - [x] 10.1 初始化 React + TypeScript + Ant Design 前端工程骨架
    - 搭建路由、API 请求层、全局状态、基础布局与异常页
    - _Requirements: 7.1, 9.1_
  - [x] 10.2 实现 Light、Auth、Dark 三套主题与主题切换机制
    - 基于 Ant Design Token 体系定义主题变量
    - _Requirements: 7.2, 7.3, 7.4_
  - [x] 10.3 实现登录页、初始化提示页与权限感知主布局
    - 登录与初始化相关页面使用 Auth 主题
    - 菜单、按钮与路由按权限动态控制可见性
    - _Requirements: 4.3, 5.1, 7.3, 8.2_
  - [x] 10.4 为主题切换与路由守卫补充最小化自动化测试
    - 覆盖三主题切换与未登录拦截
    - _Requirements: 7.2, 7.3, 8.3_

- [x] 11. 实现前端的项目、用户与权限管理页面
  - [x] 11.1 编写项目列表、项目详情、项目编辑页面
    - 支持项目基础信息编辑、存储桶绑定、CDN 地址绑定展示
    - _Requirements: 3.1, 3.2, 3.3, 7.4_
  - [x] 11.2 编写用户列表、用户编辑、禁用与项目角色绑定页面
    - 支持平台管理员维护用户基础信息与项目角色
    - _Requirements: 4.1, 4.2, 4.3, 7.4_
  - [x] 11.3 在前端实现基于角色的页面访问控制与操作禁用状态
    - 对项目只读用户禁用写操作入口
    - _Requirements: 4.5, 8.2, 8.3_
  - [x] 11.4 为用户与项目管理关键交互补充最小化自动化测试
    - 覆盖项目绑定数量校验与只读角色交互限制
    - _Requirements: 3.2, 4.2, 4.5_

- [x] 12. 实现前端的存储桶、CDN 与审计操作页面并完成联调
  - [x] 12.1 编写存储桶文件管理页面
    - 支持列表、上传、下载、删除、重命名与审计入口
    - _Requirements: 1.3, 1.4, 6.4, 7.4_
  - [x] 12.2 编写 CDN 刷新与资源同步页面
    - 支持 URL 刷新、目录刷新、同步状态反馈
    - _Requirements: 2.2, 2.3, 2.4, 7.4_
  - [x] 12.3 编写审计日志查询页面并按角色限制可见范围
    - 提供平台级与项目级查询体验
    - _Requirements: 6.2, 6.3, 6.4, 6.5_
  - [x] 12.4 完成前后端联调、统一错误提示与请求追踪号展示
    - 对上传、刷新、同步等关键失败路径显示可追踪错误信息
    - _Requirements: 2.5, 6.1, 8.3, 8.4_
  - [x] 12.5 为关键页面交互补充最小化自动化测试
    - 覆盖文件操作、刷新操作与审计过滤核心流程
    - _Requirements: 1.4, 2.2, 2.3, 6.4_

- [x] 13. 实现平台管理员重置用户密码能力（新需求，最后执行）
  - [x] 13.1 编写管理员重置用户密码后端接口与服务逻辑
    - 新增 `PUT /api/v1/users/:id/password`，仅平台管理员可调用
    - 校验目标用户存在与新密码最小长度约束，并更新密码摘要
    - _Requirements: 10.1, 10.2, 10.3_
  - [x] 13.2 在用户管理页面新增“重置密码”交互
    - 提供管理员重置目标用户密码的弹窗表单与提交流程
    - 对失败原因进行结构化提示并保持页面状态一致
    - _Requirements: 10.5, 10.2, 10.3_
  - [x] 13.3 将管理员重置密码操作纳入审计记录
    - 记录操作者、目标用户、结果与时间，覆盖成功与失败路径
    - _Requirements: 10.4, 6.1_
  - [x] 13.4 为重置密码能力补充最小化自动化测试
    - 覆盖管理员成功重置、目标用户不存在、密码策略校验失败
    - _Requirements: 10.1, 10.2, 10.3, 10.4_

- [ ] 14. 实现存储桶上传增强能力（新需求）
  - [x] 14.1 修复前端单文件上传状态丢失问题
    - 排查并修复“选择文件后点击上传仍提示请选择待上传文件”的状态同步逻辑
    - 确保文件选择控件与实际提交数据源保持一致
    - _Requirements: 1.4, 7.1, 8.3_
  - [x] 14.2 实现多文件选择与批量上传能力
    - 将前端上传控件改为支持多选文件并展示待上传列表
    - 扩展后端上传接口以支持批量文件上传请求并返回逐文件结果
    - _Requirements: 1.4, 7.1, 8.4_
  - [x] 14.3 实现压缩包自动解压并上传能力
    - 在后端上传链路中识别并解析 `zip`、`tar`、`tar.gz`、`tgz` 文件
    - 解压后按目录结构批量上传对象，并返回解压与上传结果摘要
    - _Requirements: 1.4, 8.1, 9.4_
  - [x] 14.4 实现上传大小限制配置与前端展示
    - 在后端新增上传大小限制配置项，默认值为 `20MB`，并在上传校验中生效
    - 前端在上传区域展示当前大小限制，并对超限文件进行前置拦截提示
    - _Requirements: 1.4, 7.1, 8.3_
  - [ ]* 14.5 为上传增强核心流程补充最小化自动化测试
    - 覆盖单文件上传修复、多文件上传、压缩包解压上传与大小限制校验
    - _Requirements: 1.4, 8.3, 8.4_

- [x] 15. 当前分支已落地修复与增强记录
  - [x] 15.1 修复项目创建入口缺失问题并恢复创建流程
    - 在项目页面新增“新建项目”入口与创建弹窗提交流程
    - 创建成功后自动刷新列表并选中新建项目
    - _Requirements: 3.1, 3.3, 7.1_
  - [x] 15.2 放宽项目初始绑定约束并支持后续补绑
    - 支持项目空配置创建，允许仅绑定存储桶或仅绑定 CDN
    - 数量约束调整为每类绑定 `0~2` 且在存在绑定时校验唯一 Primary
    - _Requirements: 3.1, 3.2, 8.3_
  - [x] 15.3 增强存储页项目与桶选择交互
    - Storage 页面支持按 Project ID 下拉搜索
    - 选择项目后自动加载并可选择该项目已绑定 Bucket
    - _Requirements: 1.3, 7.1_
  - [x] 15.4 接入腾讯云 COS 与腾讯 CDN Provider
    - 实现并注册腾讯 COS 对象存储 Provider 与腾讯 CDN 刷新 Provider
    - 修复 `provider_not_supported` 并完成后端全量编译测试通过
    - _Requirements: 1.2, 2.2, 2.3, 9.1, 9.4_
  - [x] 15.5 完成存储桶与 CDN 的 AK/SK 结构化凭据改造
    - 项目配置表单改为 AK/SK/Token/Region 结构化输入
    - CDN 操作链路改为使用 CDN 自身凭据执行，返回脱敏凭据展示
    - _Requirements: 1.1, 2.2, 8.1, 8.4_

- [ ] 16. 修复压缩包上传可见性并增加会话级进展与耗时展示（新需求）
  - [x] 16.1 修复压缩包上传落桶失败与 Object List 不可见问题
    - 修复压缩包上传链路中导致对象未落桶或上传后列表不可见的问题
    - 在上传完成后刷新对象列表并确保与当前项目、存储桶、前缀筛选条件一致
    - _Requirements: 11.1, 11.2, 1.3, 1.4_
  - [x] 16.2 实现压缩包上传会话标识、开始结束时间与耗时统计
    - 为每次压缩包上传生成会话标识并记录开始时间、结束时间、耗时与条目统计
    - 在上传响应中返回会话级汇总信息，支持前端展示
    - _Requirements: 11.3, 11.4_
  - [x] 16.3 增加会话级审计汇总并关联单文件审计明细
    - 记录 `object.upload_archive` 会话汇总审计并关联单文件 `object.upload` 审计
    - 支持按会话标识查询汇总与明细，覆盖成功与失败路径
    - _Requirements: 11.5, 6.1, 6.3, 6.5_
  - [x] 16.4 在存储页面展示压缩包整体进展、时间与耗时
    - 展示会话级上传进展、开始结束时间、耗时、成功失败计数与失败原因摘要
    - 支持从会话汇总查看对应明细记录
    - _Requirements: 11.4, 11.5, 7.1_
  - [ ]* 16.5 为压缩包上传会话能力补充最小化自动化测试
    - 覆盖落桶成功、部分失败、会话耗时统计、汇总审计与前端会话视图展示
    - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.5_

- [ ] 17. 增强存储桶文件页的层级浏览、分页、目录删除与批量操作（新需求）
  - [x] 17.1 修复文件列表每页条数配置不生效问题
    - 统一前后端分页参数，确保用户设置每页条数后查询结果与表格展示一致
    - 修复切换分页大小后仍固定 10 条的行为
    - _Requirements: 12.1, 1.3_
  - [x] 17.2 实现层级目录浏览与目录内查询
    - 改造存储列表接口与页面展示，当前层仅显示文件名/目录名，不显示完整路径
    - 支持点击目录进入下一层并支持返回上一级
    - _Requirements: 12.2, 12.3, 1.3_
  - [x] 17.3 实现单目录递归删除能力
    - 扩展 `DELETE /api/v1/projects/:id/storage/objects`，当目标 key 为目录前缀时分页列举并递归删除目录下全部对象
    - 返回目录级结果摘要，至少包含删除对象数、失败对象数与失败原因摘要
    - 删除当前浏览目录后，前端返回上一级目录并刷新列表
    - _Requirements: 12.4, 8.4_
  - [x] 17.4 实现支持目录的批量删除能力
    - 扩展表格多选与批量删除接口，允许当前层级同时选择文件与目录
    - 对文件执行单文件删除，对目录执行递归删除，并返回逐项结果与目录级统计
    - 在确认弹窗与结果提示中明确目录会删除其下全部对象
    - _Requirements: 12.5, 8.2, 8.4_
  - [x] 17.5 实现文件与目录重命名能力
    - 单文件重命名能力保持可用
    - 支持目录重命名，将目录前缀下对象迁移到目标目录前缀并返回迁移结果摘要
    - _Requirements: 14.1, 14.2_
  - [ ]* 17.6 为文件页增强能力补充最小化自动化测试
    - 覆盖分页大小切换、目录进入/返回、单目录递归删除、批量混合删除与目录重命名迁移结果
    - _Requirements: 12.1, 12.2, 12.3, 12.4, 12.5, 14.2_

- [ ] 18. 提升上传性能并实现阶段化进度提示（新需求）
  - [x] 18.1 实现压缩包条目并发上传能力与可配置并发度
    - 在后端压缩包上传链路引入 worker pool 并发执行对象上传
    - 在 `backend/internal/config/config.go` 的 `UploadConfig` 中新增 `archive_parallelism` 配置并设置默认值 `4`
    - 在 `backend/config.example.yaml` 增加 `upload.archive_parallelism` 示例项
    - 对 `archive_parallelism` 做边界处理，限制可用范围 `2~8`
    - 保证并发执行下会话统计与审计写入一致
    - _Requirements: 13.1, 11.3, 11.4_
  - [x] 18.2 实现上传阶段 A（浏览器到后端）进度提示
    - 在前端上传请求中接入请求传输进度回调并展示百分比
    - 对超时与取消状态提供可追踪提示信息
    - _Requirements: 13.2, 7.1_
  - [x] 18.3 实现上传阶段 B（后端处理）进度展示与会话轮询（延期）
    - 基于 `sessionId` 轮询会话汇总审计，展示进度、开始结束时间、耗时、成功失败计数
    - 支持从会话汇总跳转查看明细并聚合失败原因摘要
    - _Requirements: 13.3, 13.4, 11.5_
  - [ ] 18.4 优化单文件 `key` 与多文件 `keyPrefix` 的交互文案与参数约束（暂时取消开发）
    - 在上传表单中按上传模式展示差异化说明与提示
    - 确保单文件场景提交 `key`，多文件或压缩包场景提交 `keyPrefix`
    - 当前按产品决策暂不执行，后续如恢复开发则直接基于本任务继续
    - _Requirements: 13.5, 1.4_
  - [ ]* 18.5 为上传性能与阶段化进度补充最小化自动化测试
    - 覆盖并发上传统计一致性、阶段进度展示、会话轮询终止与参数语义校验
    - _Requirements: 13.1, 13.2, 13.3, 13.4, 13.5_

- [ ] 19. 支持项目内多云混合绑定并接入阿里云存储与 CDN（新需求）
  - [x] 19.1 调整项目绑定校验为“同项目允许多云混合”
    - 修改项目存储桶与 CDN 绑定校验逻辑，移除同项目绑定项 `provider_type` 必须一致的限制
    - 保留 `0~2` 数量约束与“存在绑定时仅一个 Primary”规则
    - _Requirements: 3.2, 15.1, 15.2_
  - [x] 19.2 在绑定保存链路增加 Provider 注册可用性校验
    - 创建或更新项目绑定时，校验每个绑定项 `provider_type` 在当前服务实例中已注册
    - 对未注册 provider 返回可定位到具体绑定项的结构化错误
    - _Requirements: 15.3, 8.4_
  - [x] 19.3 实现并注册阿里云 OSS Provider
    - 新增阿里云 OSS 适配器并实现对象列表、上传、下载、删除、重命名能力
    - 在应用启动流程注册阿里云对象存储 Provider
    - _Requirements: 1.2, 1.3, 1.4, 15.5, 9.1_
  - [x] 19.4 实现并注册阿里云 CDN Provider
    - 新增阿里云 CDN 适配器并实现 URL 刷新、目录刷新与资源同步调用能力
    - 在应用启动流程注册阿里云 CDN Provider
    - _Requirements: 2.2, 2.3, 2.4, 15.5, 9.1_
  - [x] 19.5 调整前端项目配置页与错误提示以支持多云混合绑定
    - 保持同项目可添加不同 `providerType` 的存储桶/CDN 绑定项并正常提交
    - 对 `provider_not_registered`、绑定项定位错误等后端返回做可读化提示
    - _Requirements: 3.2, 15.1, 15.2, 15.3, 7.1_
  - [ ]* 19.6 为多云绑定与阿里云接入补充最小化自动化测试
    - 覆盖多云混合绑定保存、未注册 provider 拒绝、阿里云存储操作与阿里云 CDN 刷新核心流程
    - _Requirements: 15.1, 15.2, 15.3, 15.4, 15.5_

- [x] 20. 实现项目编辑时绑定凭据默认保留能力（新需求）
  - [x] 20.1 扩展项目绑定更新协议并支持 `credentialOperation`
    - 在项目更新请求 DTO 中为存储桶/CDN 绑定项新增 `credentialOperation` 字段，支持 `KEEP` 与 `REPLACE`
    - 对已有绑定项缺省该字段时注入默认值 `KEEP`，兼容历史前端提交
    - _Requirements: 16.1, 16.2_
  - [x] 20.2 实现服务层 `KEEP`/`REPLACE` 凭据编排与校验
    - `KEEP` 模式复用已加密历史凭据并允许仅更新非凭据字段
    - `REPLACE` 模式要求完整凭据并在校验通过后覆盖历史密文
    - _Requirements: 16.1, 16.2_
  - [x] 20.3 增加 provider 变更强制替换凭据约束
    - 当已有绑定项 `providerType` 发生变化且 `credentialOperation=KEEP` 时拒绝保存
    - 返回可定位到具体绑定项的结构化错误码与字段路径
    - _Requirements: 16.3, 16.4_
  - [x] 20.4 调整前端项目配置页交互以支持凭据保留与替换
    - 已有绑定项默认展示脱敏凭据状态并关闭“更新凭据”开关
    - 开关关闭时提交 `KEEP`，开启时提交 `REPLACE` 与新凭据字段
    - _Requirements: 16.1, 16.2, 16.5_
  - [x] 20.5 完善前端错误映射与绑定项定位提示
    - 对 provider 变更必须替换凭据与凭据缺失场景返回可读化提示
    - 将错误定位到具体绑定项并保持表单状态一致
    - _Requirements: 16.4, 16.5_
  - [x] 20.6 在绑定更新链路接入凭据操作审计记录
    - 记录操作者、项目、绑定项、凭据操作模式与结果
    - _Requirements: 6.1, 8.4, 16.2, 16.4_
  - [x]* 20.7 为凭据保留能力补充最小化自动化测试
    - 覆盖已有绑定 KEEP 成功、REPLACE 成功、provider 变更 + KEEP 拒绝、绑定项错误定位提示
    - _Requirements: 16.1, 16.2, 16.3, 16.4, 16.5_

- [ ] 21. 软废弃 CDN 绑定 PurgeScope 并简化绑定字段（新需求）
  - [x] 21.1 更新 CDN 绑定术语与交互命名为“CDN 域名”
    - 前端项目配置页与详情展示统一使用“CDN 域名”文案，保持接口字段 `cdnEndpoint` 不变
    - _Requirements: 17.1_
  - [x] 21.2 调整前端 CDN 绑定表单，移除 PurgeScope 输入并将 Region 改为可选
    - 创建/编辑项目时不再收集 `purgeScope`
    - `region` 字段移除必填校验并增加“可选，默认使用 Provider fallback”提示
    - _Requirements: 17.2, 17.5_
  - [x] 21.3 调整后端项目绑定更新链路，兼容 PurgeScope 软废弃
    - CDN 绑定创建/更新不再依赖请求中的 `purgeScope`
    - 软废弃阶段继续兼容历史 `project_cdns.purge_scope` 字段读取
    - _Requirements: 17.3, 17.4_
  - [x] 21.4 对齐 CDN 刷新语义到操作接口
    - 明确 `refresh-url` 与 `refresh-directory` 由接口动作语义决定，不读取绑定级 `purgeScope`
    - _Requirements: 17.3_
  - [ ]* 21.5 为 CDN 字段简化与软废弃补充最小化自动化测试
    - 覆盖前端表单校验变化、后端绑定更新兼容、刷新动作语义保持正确
    - _Requirements: 17.2, 17.3, 17.4, 17.5_
  - [ ] 21.6 预留硬删除迁移任务占位（后续阶段执行）
    - 定义后续 migration 计划：删除 `project_cdns.purge_scope` 字段及关联兼容代码
    - 本任务仅新增占位说明，不在当前迭代执行迁移
    - _Requirements: 17.4_

- [ ] 22. 修复目录删除超时取消并实现上传/删除并发配置化（新需求）
  - [x] 22.1 扩展后端配置模型，新增上传/删除并发与删除超时配置项
    - 在 `backend/internal/config/config.go` 增加 `delete` 配置节，至少包含 `parallelism`、`batch_parallelism`、`file_parallelism`、`request_timeout_seconds`、`list_page_size`
    - 在 `upload` 配置节新增 `file_parallelism`，用于非压缩包文件上传并发
    - 为新增配置补充默认值与边界钳制，并在 `backend/config.example.yaml` 增加示例项
    - _Requirements: 1.4, 9.3, 9.4, 12.4, 12.5, 13.1_
  - [x] 22.2 修复前端目录删除与批量删除超时取消问题
    - 为 `/storage/objects` 与 `/storage/objects/batch` 请求覆盖更长超时时间并接入取消控制
    - 区分超时、主动取消、后端失败三类提示，避免统一报“删除失败”
    - _Requirements: 12.4, 12.5, 8.4_
  - [x] 22.3 实现目录递归删除并发执行与可控限流
    - 在后端目录递归删除链路引入 worker pool，并使用 `delete.parallelism` 控制并发度
    - 保持删除结果统计与失败摘要的一致性，避免重复删除与漏删
    - _Requirements: 12.4, 8.4, 9.4_
  - [x] 22.4 实现单文件删除并发能力
    - 在批量删除链路中对“文件 key”使用 `delete.file_parallelism` 并发执行删除
    - 保持单文件删除结果与错误码稳定返回，避免并发下统计偏差
    - _Requirements: 12.5, 8.4, 9.4_
  - [x] 22.5 实现批量混合删除并发编排与结果稳定返回
    - 在批量删除链路使用 `delete.batch_parallelism` 并发处理 key（文件删除与目录递归删除混合场景）
    - 返回结果需保持可追踪（包含目标类型、成功失败计数、错误摘要）
    - _Requirements: 12.5, 8.4, 9.4_
  - [x] 22.6 实现目录删除请求超时控制与错误映射
    - 基于 `delete.request_timeout_seconds` 对 Provider 调用链路施加超时上下文
    - 将超时与取消映射为可识别错误码并透传到前端提示
    - _Requirements: 12.4, 12.5, 9.4_
  - [x] 22.7 扩展上传并发配置到单文件/多文件非压缩上传链路
    - 使用 `upload.file_parallelism` 在非压缩包上传路径启用并发上传，覆盖单文件与多文件场景
    - 保持与 `upload.archive_parallelism` 的职责边界，避免统计口径冲突
    - _Requirements: 1.4, 13.1, 8.4_
  - [x] 22.8 修复 Archive Upload Sessions 表格长文本撑宽问题并保持布局稳定
    - 对“会话 ID”“失败摘要”等长文本列启用省略显示与悬浮提示，限制列宽并启用固定表格布局
    - 保持表格在长文本场景下不扩展容器宽度，必要时仅在表格区域横向滚动
    - _Requirements: 7.1, 11.5, 13.4_
  - [x] 22.9 调整存储页区块顺序，将 Object List 提升到 Archive Upload Sessions 之上
    - 在 `StoragePage` 中重排卡片布局：`Upload` -> `Object List` -> `Archive Upload Sessions`
    - 保持现有查询、会话刷新、明细抽屉等交互能力不回归
    - _Requirements: 7.1, 12.2, 12.3, 11.5_
  - [ ]* 22.10 为删除并发、超时取消、上传并发与会话表格布局补充最小化自动化测试
    - 覆盖目录递归删除超时、主动取消、单文件并发删除、批量混合删除结果一致性、单文件/多文件并发上传统计一致性
    - 覆盖会话表格长文本不撑宽与区块重排后的核心交互可用性
    - _Requirements: 1.4, 7.1, 11.5, 12.4, 12.5, 13.1, 8.4, 9.4_

- [ ] 23. 优化 CDN 操作台为“项目与绑定下拉驱动”的统一操作页面（新需求）
  - [x] 23.1 新增 CDN 页面项目下拉与绑定选项加载能力
    - 复用项目列表接口加载用户可见项目选项，并在选择项目后加载该项目绑定的 CDN 域名与存储桶列表
    - 统一维护 `projectId`、`cdnEndpoint`、`bucketName` 的页面级共享状态
    - _Requirements: 18.1, 18.2, 18.3_
  - [x] 23.2 实现 CDN 域名与存储桶默认值策略
    - 项目切换后默认选中 Primary CDN 域名与 Primary Bucket；无 Primary 时回退到首个可选项
    - 绑定列表为空时展示可读提示并禁用对应提交按钮
    - _Requirements: 18.2, 18.3, 18.5_
  - [x] 23.3 重构 URL 刷新、目录刷新、资源同步为共享上下文操作区
    - 保留三类操作在同一页面执行，减少重复字段输入与重复校验
    - 切换操作类型时复用当前项目与绑定选择状态
    - _Requirements: 18.4, 2.2, 2.3, 2.4_
  - [x] 23.4 调整资源同步表单为存储桶下拉选择
    - 移除手工输入 `bucketName`，改为基于项目绑定的 Bucket 下拉
    - 保持后端请求载荷字段兼容与错误映射一致
    - _Requirements: 18.3, 18.5, 2.4_
  - [x] 23.5 完善表单校验与错误提示定位
    - 对项目、CDN 域名、存储桶、URL/目录/路径输入提供字段级校验提示
    - 在绑定缺失或上下文无效时阻止提交并给出可操作提示
    - _Requirements: 18.5, 8.3, 8.4_
  - [ ]* 23.6 为 CDN 操作台交互优化补充最小化自动化测试
    - 覆盖项目切换、CDN 下拉默认值、Bucket 下拉默认值、三类操作共享上下文与提交参数正确性
    - _Requirements: 18.1, 18.2, 18.3, 18.4, 18.5_

- [ ] 24. 优化审计查询筛选为下拉选项并减少手工输入（新需求）
  - [x] 24.1 新增审计筛选选项查询接口
    - 新增平台级筛选选项接口，返回 Action 与 Target Type 可选值集合
    - 新增项目级筛选选项接口，返回 Project ID、Action 与 Target Type 可选值集合并按当前用户可见范围过滤
    - _Requirements: 19.1, 19.2, 6.2, 6.3_
  - [x] 24.2 改造平台级审计查询表单为下拉筛选
    - 将 Action 与 Target Type 从手工输入改为可搜索下拉并支持清空
    - 下拉选项为空时展示可读提示并保持查询按钮可用
    - _Requirements: 19.1, 19.4, 19.5_
  - [x] 24.3 改造项目级审计查询表单为下拉筛选
    - 将 Project ID、Action、Target Type 改为可搜索下拉并支持清空
    - Project ID 选项展示 `projectId + projectName`，并在切换项目时联动更新 Action 与 Target Type 选项
    - _Requirements: 19.2, 19.3, 19.5_
  - [x] 24.4 统一下拉筛选参数映射与错误提示定位
    - 在查询请求中仅提交已选下拉项，清空项不传对应参数
    - 对无效筛选上下文、选项加载失败与查询失败提供字段级提示与全局错误提示
    - _Requirements: 19.3, 19.4, 19.5, 8.3, 8.4_
  - [ ]* 24.5 为审计筛选下拉改造补充最小化自动化测试
    - 覆盖平台级 Action/Target Type 下拉筛选、项目级 Project ID/Action/Target Type 联动、清空筛选后的参数回退与空选项提示
    - _Requirements: 19.1, 19.2, 19.3, 19.4, 19.5_

- [ ] 25. 修复项目角色用户在存储与 CDN 页面不可见不可操作问题（新需求）
  - [x] 25.1 新增“当前用户可见项目列表”与“项目上下文”后端接口
    - 新增 `GET /api/v1/projects/accessible`，平台管理员返回全部项目，标准用户返回已绑定项目
    - 新增 `GET /api/v1/projects/:id/context`，返回项目 `buckets` 与 `cdns`，并通过项目作用域校验
    - _Requirements: 20.1, 20.2, 20.3, 20.5_
  - [x] 25.2 调整前端 Storage 页面改用可见项目与项目上下文接口
    - 项目下拉改为加载 `accessible` 列表
    - Bucket 选项改为从 `context` 加载，避免依赖平台管理员接口
    - _Requirements: 20.1, 20.3, 18.1, 18.3_
  - [x] 25.3 调整前端 CDN 页面改用可见项目与项目上下文接口
    - 项目下拉改为加载 `accessible` 列表
    - CDN 与 Bucket 选项改为从 `context` 加载，保证项目角色用户可见
    - _Requirements: 20.1, 20.3, 18.1, 18.2, 18.3_
  - [x] 25.4 修正前端写权限判定为“项目角色 + 平台角色联合判定”
    - 页面写操作按钮不再仅依据 `platformRole` 判定
    - 对 `project_admin` 放开项目写操作，对 `project_read_only` 保持只读
    - _Requirements: 20.4, 4.2, 4.5, 8.2_
  - [ ]* 25.5 为项目角色可见性与写权限修复补充最小化自动化测试
    - 覆盖标准用户可见项目加载、项目上下文授权拒绝、Storage/CDN 页面对项目管理员与项目只读用户的写入口状态
    - _Requirements: 20.1, 20.3, 20.4, 20.5_

- [ ] 26. 为 CDN 页面增加目录查询能力并对齐项目读写权限（新需求）
  - [x] 26.1 新增 CDN 目录查询后端接口并接入项目读权限
    - 新增 `GET /api/v1/projects/:id/cdns/directories`，要求参数至少包含 `bucketName`，可选 `prefix`
    - 复用项目作用域校验与 `RequireProjectRead`，返回目录候选列表
    - _Requirements: 21.1, 21.4, 21.5_
  - [x] 26.2 实现 CDN 目录查询服务编排与目录聚合逻辑
    - 基于项目上下文解析目标存储桶，调用存储对象列表能力聚合目录候选
    - 对空结果、非法参数、越权访问返回稳定错误码与提示信息
    - _Requirements: 21.1, 21.4, 21.5_
  - [x] 26.3 在 CDN 页面增加目录查询交互并用于目录刷新输入
    - 增加目录查询入口与结果列表，支持按项目和存储桶加载目录候选
    - 保持用户可手工输入目录，并支持从查询结果快捷填充
    - _Requirements: 21.1, 21.4_
  - [x] 26.4 对齐 CDN 页面读写权限：只读可查目录、管理员可提交写操作
    - `project_read_only` 可见并可用目录查询入口，但 URL 刷新、目录刷新、资源同步按钮禁用
    - `project_admin` 与平台管理员可执行目录查询与写操作
    - _Requirements: 21.2, 21.3_
  - [ ]* 26.5 为目录查询与读写权限分离补充最小化自动化测试
    - 覆盖目录查询参数校验、越权拒绝、项目只读可查询但不可提交写操作、项目管理员可查询且可提交写操作
    - _Requirements: 21.2, 21.3, 21.4, 21.5_

- [x] 27. 优化用户项目角色绑定弹框为“快照预填 + 紧凑编辑”（新需求）
  - [x] 27.1 新增用户项目绑定快照查询后端接口
    - 新增 `GET /api/v1/users/:id/project-bindings`，返回目标用户当前绑定集合
    - 接口仅允许平台管理员访问，目标用户不存在时返回 `user_not_found`
    - _Requirements: 22.1, 22.5_
  - [x] 27.2 在用户绑定弹框打开时加载并回填已有绑定
    - `openBindings` 时调用快照接口并将结果写入表单初始值
    - 保留保存接口为 `PUT /users/:id/project-bindings` 的整体替换语义
    - _Requirements: 22.1, 22.2, 22.5_
  - [x] 27.3 将绑定编辑区改造为紧凑绑定表格
    - 以行式编辑替换卡片堆叠，单行展示项目、角色、删除操作
    - 保留新增绑定行与重复项目校验逻辑
    - _Requirements: 22.3, 22.4_
  - [x] 27.4 保存成功后刷新并回填最新绑定状态
    - 保存成功后使用接口返回值或重新拉取快照回填表单
    - 关闭并再次打开弹框时展示上次保存结果，不出现空白重录
    - _Requirements: 22.2, 22.5_
  - [ ]* 27.5 为绑定快照与紧凑编辑补充最小化自动化测试
    - 覆盖快照预填、重复项目拒绝、保存后回填、二次打开保持最新绑定状态
    - _Requirements: 22.1, 22.2, 22.4, 22.5_

- [x] 28. 实现 Overview 首页“业务工作台 + 运维风险摘要”能力（新需求）
  - [x] 28.1 新增 Overview 页面路由与骨架布局
    - 在前端新增 Overview 页面，包含欢迎区、时间范围筛选、刷新入口与快捷操作区
    - 配置默认入口为 Overview，并保持现有路由守卫与权限机制
    - _Requirements: 23.2, 7.1, 8.2_
  - [x] 28.2 实现业务核心卡片数据聚合与展示
    - 基于可见项目范围聚合上传会话总数、上传失败数、CDN 操作总数、CDN 失败数、可见项目数
    - 支持 `24h/7d` 时间范围切换与手动刷新
    - _Requirements: 23.1, 20.1, 20.2, 20.3_
  - [x] 28.3 实现项目工作台列表与快捷跳转
    - 展示项目维度的主 Bucket、主 CDN、最近上传时间、最近刷新时间与失败计数
    - 每行提供进入 Storage 与 CDN 页面的快捷入口
    - _Requirements: 23.3, 20.3, 18.1, 18.2, 18.3_
  - [x] 28.4 实现运维风险摘要区域与筛选跳转
    - 展示最近失败上传会话与最近失败 CDN 操作，并包含时间与失败摘要
    - 点击风险项跳转到带筛选条件的审计结果或目标项目页面
    - _Requirements: 23.4, 6.2, 6.3, 19.3, 19.5_
  - [x] 28.5 实现最近活动时间线与 requestId 追踪入口
    - 展示最近关键操作的 actor、project、action、result、time、requestId
    - 支持按角色与项目范围限制可见数据
    - _Requirements: 23.4, 6.2, 6.3, 6.5, 8.3_
  - [x] 28.6 对齐只读角色可见性与写入口禁用策略
    - 在 Overview 对 `project_read_only` 隐藏写操作快捷入口，保留查询与导航入口
    - 确保平台管理员与项目管理员保留完整工作台操作入口
    - _Requirements: 23.5, 4.2, 4.5, 8.2_
  - [ ]* 28.7 为 Overview 核心交互补充最小化自动化测试
    - 覆盖卡片聚合、时间范围切换、风险跳转、只读角色写入口隐藏与活动时间线展示
    - _Requirements: 23.1, 23.2, 23.3, 23.4, 23.5_

- [x] 29. 实现 Neo-Tech 主题系统升级（新需求）
  - [x] 29.1 建立统一主题令牌与样式基线
    - 在前端主题层定义 Neo-Tech 语义令牌与关键组件映射规则
    - 对齐 Light、Auth、Dark 三套主题的颜色、边框、阴影与状态语义
    - _Requirements: 24.1, 24.2_
  - [x] 29.2 重做 Light 主题为科技风高可读样式
    - 调整页面背景、卡片层级、表格与表单控件对比度
    - 保持业务密集页面信息可读性优先
    - _Requirements: 24.2, 24.5_
  - [x] 29.3 重做 Auth 主题的独立视觉风格
    - 调整登录与初始化页面为认证场景专属科技风样式
    - 保持认证流程交互语义与行为不变
    - _Requirements: 24.3, 7.3_
  - [x] 29.4 对齐 Dark 主题细节与三主题一致性
    - 统一按钮、标签、告警、表格状态在 Dark 主题下的视觉语义
    - 消除三主题间状态色和层级语义不一致问题
    - _Requirements: 24.2, 24.5_
  - [x] 29.5 实现动效分级与降级机制
    - 为页面进入、悬停与状态切换提供标准动效
    - 在低性能或动效受限场景下自动降级非关键动画
    - _Requirements: 24.4_
  - [ ]* 29.6 为主题系统升级补充最小化自动化测试
    - 覆盖三主题切换、关键组件对比度基线与动效降级开关行为
    - _Requirements: 24.2, 24.4, 24.5_

- [x] 30. 实现国际化能力（默认 zh-CN，可切换 en-US）（新需求）
  - [x] 30.1 接入前端 i18n 基础设施与语言上下文
    - 引入 i18n Provider 并在应用入口挂载
    - 配置默认语言 `zh-CN` 与可选语言 `en-US`
    - _Requirements: 25.1, 25.4_
  - [x] 30.2 实现顶栏语言切换与持久化
    - 在主布局加入语言切换入口
    - 将语言选择持久化并在刷新或重新登录后恢复
    - _Requirements: 25.2, 25.3_
  - [x] 30.3 迁移 Layout、Overview 与 Auth 页面文案到 i18n
    - 替换硬编码导航、标题、按钮与提示文案
    - 保证默认中文显示并支持即时英文切换
    - _Requirements: 25.1, 25.2, 25.4_
  - [x] 30.4 迁移 Audits、Storage 与 CDN 页面核心文案到 i18n
    - 覆盖查询表单、结果提示、错误提示与关键状态文案
    - _Requirements: 25.2, 25.4_
  - [x] 30.5 迁移 Users 与 Projects 页面核心文案到 i18n
    - 覆盖管理页主要操作文案与状态提示
    - _Requirements: 25.2, 25.4_
  - [x] 30.6 实现缺失键回退与可追踪告警记录
    - 当前语言键缺失时回退 `zh-CN` 文案
    - 输出可追踪告警用于补齐翻译资源
    - _Requirements: 25.5_
  - [ ]* 30.7 为国际化切换补充最小化自动化测试
    - 覆盖默认中文、切换 `en-US`、刷新后持久化与缺失键回退行为
    - _Requirements: 25.1, 25.2, 25.3, 25.5_

- [ ] 31. 扩展 Overview 首页指标统计与图表能力（新需求）
  - [x] 31.1 实现 Overview 指标聚合后端接口
    - 新增 `GET /api/v1/overview/metrics`，支持 `timeWindow=24h|7d|30d`
    - 聚合可见项目范围内 KPI：项目总数、存储桶总数、CDN 总数、上传会话总数、CDN 操作总数、失败总数、平均上传耗时
    - 返回上传趋势与 CDN 操作趋势序列，并提供空状态标记
    - _Requirements: 26.1, 26.2, 26.4, 26.5_
  - [x] 31.2 实现云厂商占比与操作类型占比聚合
    - 聚合云厂商资源占比（基于存储桶与 CDN 绑定的 `provider_type`）
    - 聚合操作类型占比（基于审计 `action`），低频项合并为 `other`
    - _Requirements: 26.3, 26.4, 26.5_
  - [x] 31.3 改造 Overview 前端 KPI 卡片展示
    - 在现有 Overview 卡片区接入新增 KPI 字段并支持时间窗口联动刷新
    - 对无数据状态显示零值与空状态提示，不阻断页面操作
    - _Requirements: 26.1, 26.2, 26.5_
  - [x] 31.4 在 Overview 前端接入折线图与扇形图组件
    - 增加上传趋势、CDN 操作趋势折线图
    - 增加云厂商占比、操作类型占比扇形图，并处理分类过多场景
    - _Requirements: 26.2, 26.3, 26.5_
  - [x] 31.5 对齐权限范围与错误处理
    - 确保平台管理员与项目角色用户均按可见项目范围查看指标
    - 对非法 `timeWindow`、数据查询失败、空数据场景提供稳定错误映射和提示
    - _Requirements: 26.4, 26.5, 20.1, 20.2, 20.3_
  - [ ]* 31.6 为 Overview 指标与图表补充最小化自动化测试
    - 覆盖时间窗口切换、可见范围过滤、图表空状态与 `other` 合并逻辑
    - _Requirements: 26.1, 26.2, 26.3, 26.4, 26.5_
